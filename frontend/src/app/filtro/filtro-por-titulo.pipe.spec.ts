import { FiltroPorTituloPipe } from './filtro-por-titulo.pipe';

describe('FiltroPorTituloPipe', () => {
  it('create an instance', () => {
    const pipe = new FiltroPorTituloPipe();
    expect(pipe).toBeTruthy();
  });
});
